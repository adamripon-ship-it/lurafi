# v0 component prompt — App showcase

Build `AppSection` id="app" — black background section.

**Header:** Overline "Kevin App" (`#2997ff`), H2 white "Presence on your schedule.", subhead `#a1a1a6`.

**Phone showcase:** Two iPhone mockups in horizontal carousel (mobile) / side-by-side (desktop). Captions below each:
- "Protected — arm and disarm from anywhere"
- "Schedule — set protection by day and hour"

Use provided CloudFront mockup URLs. Frame with subtle shadow.

**Feature tiles (4):** 2×2 grid @768px, dark cards on black or subtle border:
1. Weekly schedules
2. Geo-aware activation
3. Your own sounds
4. Multiple places

Optional: subtle parallax/scroll animation on phones. Carousel needs prev/next + aria-roledescription="carousel".

Reference `app-section.png`.
