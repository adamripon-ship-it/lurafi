{%- liquid
  assign lurafi_hero_lcp_url = 'kevin-hero-product-front.png' | asset_url
-%}
{%- comment -%}
  Preload retail Kevin product photo (slide 1) for LCP.
{%- endcomment -%}
<link rel="preload" as="image" href="{{ lurafi_hero_lcp_url }}" fetchpriority="high">
