{%- liquid
  assign lurafi_hero_lcp_url = 'kevin-hero-burglary-prevention.jpg' | asset_url
-%}
{%- comment -%}
  Preload default Kevin device image for homepage LCP. Matches hero slide 1 fallback.
{%- endcomment -%}
<link rel="preload" as="image" href="{{ lurafi_hero_lcp_url }}" fetchpriority="high">
