## Summary

<!-- What changed on the Shopify theme? -->

## Store impact

- [ ] Checked on a **development theme** or preview URL (not live-only)
- [ ] `locales/*` updated if copy changed (`npm run locales:build`)
- [ ] No secrets in diff (`.env`, CLI tokens, `settings_data` merchant tokens)

## Test plan

- [ ] `npm run theme:check`
- [ ] Spot-check https://lurafi.ai/ (EN) and one locale prefix (e.g. `/nl/`)
- [ ] Configure page + cart drawer if touched
