{%- liquid
  capture configure_buy_url
    render 'configure-url', plan: 'buy'
  endcapture
  assign configure_buy_url = configure_buy_url | strip
  if cart.item_count == 1
    assign cart_label = 'accessibility.cart_item' | t: count: cart.item_count
  else
    assign cart_label = 'accessibility.cart_items' | t: count: cart.item_count
  endif
  assign header_cta = 'header.buy_now' | t: default: section.settings.cta_label
-%}

<header class="site-header" role="banner" data-theme="dark">
  <div class="site-header__inner">
    <div class="site-header__brand" data-header-zone="brand">
      <a href="{{ routes.root_url }}" class="site-header__logo" aria-label="{{ shop.name | escape }} home">{{ shop.name | default: 'Kevin' | escape }}</a>
    </div>

    <div class="site-header__divider" aria-hidden="true" data-header-zone="divider"></div>

    <nav class="site-nav" aria-label="{{ 'accessibility.main_navigation' | t }}" data-header-zone="nav">
      <a href="{{ routes.root_url }}#problem" class="site-nav__link">{{ 'header.why_kevin' | t }}</a>
      <a href="{{ routes.root_url }}#product" class="site-nav__link">{{ 'header.product' | t }}</a>
      <a href="{{ routes.root_url }}#how-it-works" class="site-nav__link">{{ 'header.how_it_works' | t }}</a>
      <a href="{{ routes.root_url }}#app" class="site-nav__link">{{ 'header.app' | t }}</a>
      <a href="{{ routes.root_url }}#pricing" class="site-nav__link">{{ 'header.pricing' | t }}</a>
    </nav>

    <div class="site-header__utilities" data-header-zone="utilities">
      <div class="site-header__lang">
        {% render 'language-selector', form_id: 'HeaderLanguageForm', selector_id: 'HeaderLanguageSelector', country_selector_id: 'HeaderCountrySelector' %}
      </div>

      <div class="site-header__actions">
        {%- if customer -%}
          <a href="{{ routes.account_url }}" class="site-header__account-link">{{ 'header.account' | t }}</a>
        {%- else -%}
          <a href="{{ routes.account_login_url }}" class="site-header__account-link">{{ 'header.log_in' | t }}</a>
        {%- endif -%}
        <a href="{{ configure_buy_url }}" class="btn-apple-primary btn-apple-primary--sm site-header__cta site-header__cta--mobile">
          {{ 'header.buy' | t }}
        </a>
        <button type="button" class="site-header__icon site-header__cart" aria-label="{{ cart_label | escape }}" data-cart-drawer-open>
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.4" aria-hidden="true">
            <path d="M3.5 5h11l-1.1 8.4a1.5 1.5 0 0 1-1.5 1.3H6.1a1.5 1.5 0 0 1-1.5-1.3L3.5 5z"/>
            <path d="M6.5 5V3.75a2.5 2.5 0 0 1 5 0V5"/>
          </svg>
          <span class="site-header__cart-count" data-cart-count {% if cart.item_count == 0 %}hidden{% endif %}>{{ cart.item_count }}</span>
        </button>
        <button type="button" class="site-header__menu-btn" aria-label="{{ 'accessibility.open_menu' | t }}" aria-expanded="false" aria-controls="MobileNavPanel" data-menu-open>
          <span class="site-header__menu-icon" aria-hidden="true">
            <span></span><span></span><span></span>
          </span>
        </button>
        <a href="{{ configure_buy_url }}" class="btn-apple-primary btn-apple-primary--sm site-header__cta site-header__cta--desktop">
          {{ header_cta | escape }}
        </a>
      </div>
    </div>
  </div>
</header>

<div class="mobile-nav" id="MobileNav" data-open="false" aria-hidden="true" hidden>
  <div class="mobile-nav__backdrop" data-menu-close tabindex="-1" aria-hidden="true"></div>
  <div
    class="mobile-nav__panel"
    id="MobileNavPanel"
    role="dialog"
    aria-modal="true"
    aria-labelledby="MobileNavBrand"
    tabindex="-1"
  >
    <div class="mobile-nav__header">
      <a href="{{ routes.root_url }}" id="MobileNavBrand" class="mobile-nav__brand" data-menu-close>{{ shop.name | default: 'Kevin' | escape }}</a>
      <button type="button" class="mobile-nav__close" data-menu-close aria-label="{{ 'accessibility.close_menu' | t }}">
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
          <path d="M1 1l12 12M13 1L1 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </button>
    </div>
    <div class="mobile-nav__lang">
      <p class="mobile-nav__group-label">{{ 'language.label' | t }}</p>
      {% render 'language-selector', form_id: 'MobileNavLanguageForm', selector_id: 'MobileNavLanguageSelector', country_selector_id: 'MobileNavCountrySelector' %}
    </div>
    <nav class="mobile-nav__links" aria-label="{{ 'accessibility.mobile_navigation' | t }}">
      <p class="mobile-nav__group-label">{{ 'header.explore' | t }}</p>
      <a href="{{ routes.root_url }}#problem" class="mobile-nav__link" data-menu-close>{{ 'header.why_kevin' | t }}</a>
      <a href="{{ routes.root_url }}#product" class="mobile-nav__link" data-menu-close>{{ 'header.product' | t }}</a>
      <a href="{{ routes.root_url }}#how-it-works" class="mobile-nav__link" data-menu-close>{{ 'header.how_it_works' | t }}</a>
      <a href="{{ routes.root_url }}#app" class="mobile-nav__link" data-menu-close>{{ 'header.app' | t }}</a>
      <p class="mobile-nav__group-label">{{ 'header.buy_group' | t }}</p>
      <a href="{{ routes.root_url }}#pricing" class="mobile-nav__link" data-menu-close>{{ 'header.pricing' | t }}</a>
      <button type="button" class="mobile-nav__link mobile-nav__cart-link" aria-label="{{ cart_label | escape }}" data-cart-drawer-open data-menu-close>{{ 'header.cart' | t }}{% if cart.item_count > 0 %} ({{ cart.item_count }}){% endif %}</button>
      <p class="mobile-nav__group-label">{{ 'header.support' | t }}</p>
      <a href="{{ routes.root_url }}#app" class="mobile-nav__link" data-menu-close>{{ 'header.setup_and_app' | t }}</a>
      {%- if customer -%}
        <a href="{{ routes.account_url }}" class="mobile-nav__link" data-menu-close>{{ 'header.account' | t }}</a>
      {%- else -%}
        <a href="{{ routes.account_login_url }}" class="mobile-nav__link" data-menu-close>{{ 'header.log_in' | t }}</a>
      {%- endif -%}
    </nav>
    <div class="mobile-nav__footer">
      <a href="{{ configure_buy_url }}" class="btn-apple-primary btn-block mobile-nav__buy" data-menu-close>{{ header_cta | escape }}</a>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Header",
  "settings": [
    { "type": "text", "id": "cta_label", "label": "Button label", "default": "Buy Now" }
  ]
}
{% endschema %}
