{%- capture configure_url -%}
  {%- render 'configure-url', plan: 'buy' -%}
{%- endcapture -%}
{%- assign configure_url = configure_url | strip -%}

<section class="section-apple bg-black">
  <div class="apple-container text-center">
    <h2 class="headline-section text-white" style="margin-bottom:16px">{%- if section.settings.heading != blank -%}{{ section.settings.heading }}{%- else -%}{{ 'home.cta.heading' | t }}{%- endif -%}</h2>
    <p class="body-elevated text-muted-dark" style="max-width:500px;margin:0 auto 32px">{%- if section.settings.subheading != blank -%}{{ section.settings.subheading }}{%- else -%}{{ 'home.cta.subheading' | t }}{%- endif -%}</p>
    <div class="cta-row">
      <a href="{{ configure_url }}" class="cta-white-btn">{%- if section.settings.primary_cta != blank -%}{{ section.settings.primary_cta }}{%- else -%}{{ 'home.cta.primary_cta' | t }}{%- endif -%}</a>
      <a href="{{ routes.root_url }}#how-it-works" class="cta-outline-btn">{%- if section.settings.secondary_cta != blank -%}{{ section.settings.secondary_cta }}{%- else -%}{{ 'home.cta.secondary_cta' | t }}{%- endif -%}</a>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Final CTA",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Someone's always home." },
    { "type": "textarea", "id": "subheading", "label": "Subheading", "default": "Travel, work late, or lock up for the night—without rehearsing worst cases. Free delivery. 60-second setup. Swiss precision." },
    { "type": "text", "id": "primary_cta", "label": "Primary CTA", "default": "Buy Kevin" },
    { "type": "text", "id": "secondary_cta", "label": "Secondary CTA", "default": "Learn more" }
  ],
  "presets": [{ "name": "Final CTA" }]
}
{% endschema %}
